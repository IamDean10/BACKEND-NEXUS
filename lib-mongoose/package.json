{
  "name": "lib-mongoose",
  "version": "1.0.0",
  "description": "shared Mongoose library for back-end projects.",
  "main": "build/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "author": "Jan Dean (jan@activeplace.com)",
  "license": "ISC",
  "dependencies": {
    "mongoose": "^6"
  },
  "devDependencies": {
    "gts": "^3.1.0",
    "ts-node": "^10.4.0",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.4.4",
    "@types/node": "^16.11.6"
  }
}
